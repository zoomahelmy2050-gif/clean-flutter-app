# Backend server URL
API_BASE_URL=http://192.168.100.21:3000

# Copy this file to .env (do NOT commit .env)
# Web OAuth 2.0 Client ID from Google Cloud Console (APIs & Services -> Credentials)
GOOGLE_WEB_CLIENT_ID=951850160974-frhtseulen7rp2hfceesr4onnop5t7mb.apps.googleusercontent.com

# Google reCAPTCHA v2 Checkbox site key (client-side)
RECAPTCHA_SITE_KEY=

# Optional: reCAPTCHA secret (server-side verification). Not used in this client-only demo.
# RECAPTCHA_SECRET=

# SMTP settings for sending OTP emails (use a Gmail App Password)
# Example for Gmail:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USERNAME=your_email@gmail.com
#   SMTP_PASSWORD=your_16_char_app_password
#   OTP_FROM_EMAIL=your_email@gmail.com
#   OTP_FROM_NAME=Your App Name
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=hazemrabiehelmyabdelmaksoud@gmail.com
SMTP_PASSWORD=tzft iavx znlt ijae
OTP_FROM_EMAIL=hazemrabiehelmyabdelmaksoud@gmail.com
OTP_FROM_NAME=Security Center

# Optional: unified FROM for all mails (summaries + OTP). If set, overrides OTP_FROM_EMAIL in summaries.
SMTP_FROM=hazemrabiehelmyabdelmaksoud@gmail.com
SMTP_FROM_NAME=Security Center

# OTP delivery mode: 'email' (default) or 'console' for local testing
OTP_DELIVERY=email

# ========== Security Summary Email Settings ==========
# Primary recipients (comma or semicolon separated). If empty, defaults to admin mailbox in code.
SUMMARY_EMAIL_TO=hazemrabiehelmyabdelmaksoud@gmail.com
# CC and BCC lists (optional, comma/semicolon separated)
SUMMARY_EMAIL_CC=
SUMMARY_EMAIL_BCC=

# Subject prefix for summary emails
SUMMARY_SUBJECT_PREFIX=Security Summary

# Summary email frequency: daily, weekly, monthly
SUMMARY_EMAIL_FREQUENCY=daily

# Enable/disable summary emails
SUMMARY_EMAIL_ENABLED=true
